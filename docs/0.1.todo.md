23.检查日志是否合理，删除日志可以可以重建，但是没有日志自动归档。


3.是否可以启用langgraph的stream输出模式，可以增加一个api,实时的返回正在执行的行为，而且这个行为可以传递给前端。
4.检查并发的异步执行的问题。
5.下载数据的问题，我要解决点击按钮下载csv文件的问题。
6.streamlit是否支持转图形。
7.升级langchain和langgraph到最新的版本，并保证当前功能正确运行。
9.对复杂问题的拆分是否还能进行优化?
10.当llm启用thinking的时候，是否会报错？


----
1.需要验证更为复杂的日期情况，比如上旬，下旬等。同比，环比等等？

4.需要核对设计文档的html流程图，或者codemap等。


7.SQL生成子图重试时，它的上下文与第一次应该是不同的，需要注意是否能获取到retry的次数，然后使用不同的提示词。
8.当前是否有checkpoint机制？使用什么作为它的key.
9.Summary Agent 节点是否有必要？它是否可以与SQL执行子图合并？因为要考虑一个问题，如果返回的数据非常大，我们可以采用分页的方式来显示，这时就没有Summary Agent了。也许我在这里需要一个HIL,让人为介入进来,选择是否生成文件下载。无法展示内容。
16.需要整理设计父图和子图的state，以及它们的调度方式。
 -如果子图作为父图里的一个节点运行，且父图开启了检查点，那么子图会共享父图的同一套检查点与 thread_id（等价于“有检查点”）
17.要整理日志的配置文件,统一日志，还是父图和子图的日志管理?
18.现在yaml配置文件有点多，需要整理一下它们的位置。
19.检查代码中的子图，它是否会在提示词中添加当前日期？或者在某些步骤中获取当前日志的值？如果我问上个月的销售额，它会怎么计算当前日期?
20.检查并行查询的时候，复杂路径是否会出错。
21.尝试更复杂的问题。


****** 已经完成的工作 ******
-- 3.当前子图中，是否已经完成了验证代码的开发？是的，要增加一些危险词的配置项目。
-- 10.把SQL生成模块的State打印出来看看它的值?
-- 11.日志中提示词中，没有包含维度表。
-- 12.为什么在state中包含了这么多的向量值？
-- 15.需要给state继续瘦身。
-- 22.设计父图的checkpoint，以及裁剪机制。包括父图的history chat如何传递给SQL生成子图?
-- 6.SQL生成子图重试时的上下文中，应该要包括上一次的失败信息，这个如何在state中进行实现和传递？
-- 2.在子图中，State 里面如何保存对话历史和上下文State? 子图每次被调用时需要
-- 1.检查三个yaml文件的配置。
-- 2.我要检查llm的调用节点，如何配置不同的节点使用不同的llm?
-- 5.下面三个yaml文件格式的划分：
-- src\configs\config.yaml
-- src\configs\logging.yaml
-- src\modules\sql_generation\config\sql_generation_subgraph.yaml

