我希望在 --step json_llm 的命令中，为json_llm的json文件增加一个主题名称(table_profile.table_domains)和一个(table_profile.table_type)。其实，table_type就等同于原来的 table_category，将来会迁移代码，把table_category 改为 table_type.
1.table_profile.table_domains 的生成：
a.) 我希望能有一个yaml文件作为指导，这个yaml文件应该包含两部分内容：一部分是对整个数据库数据范围的描述，比如"这是一个学校的教学数据库系统，里面包括了学生基本信息、教师基本信息、历史成绩查询等方面的数据"。另一部分是基于上面的数据库范围概述生成的数据主题的列表，比如"-学生基本信息类，-教师基本信息类，-历史成绩查询类"，我们后面需要把这个列表的条目作为表的table_profile.table_domains。
b.) 这个yaml文件的第一部分，必须存在。第二部分，可以在命令行中添加参数，要求它根据第一部分的范围描述，生成表的数据主题的类别列表。
c.) 有了这个yaml文件之后，我们在执行 --step json_llm 的命令时，为json_llm的目录下的json文件生成 (table_profile.table_domains)和(table_profile.table_type)。
d.) 这个yaml文件放在 ./config/metaweave/目录下，文件名称为table_domains.yaml，或者对yaml的名字你有更好的建议。

2.在执行 --step rel_llm 的时候，可以添加一个参数，比如domain, 这样在生成关系的时候，就可以先从yaml文件中读取 domain 的列表，然后遍历所有属于这个domain的表的json，两两传递给LLM生成 rel/ ，这个过程与当前相同。
3.在执行 --step cql_llm 的时候，把 table_profile.table_domains 和 table_profile.table_type 属性写入到 neo4j的表的节点中。对于不使用llm的json，也能兼容，找不到这两个属性的时候，就不写入到 cql。

