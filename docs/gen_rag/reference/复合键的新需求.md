现在我想要增加一个复合键的需求，我说的复合键主要包括下面的内容：
1.复合主键。
2.复合索引。
3.复合外键约束。
4.复合唯一约束。

对于这些复合键，我想给它们找到合适的关联关系：
1.第一步，根据名字和类型找出目标表，逐个字段比对目标表与源表的复合键，这些字段的名字相似度>=0.8,类型相似度>=0.8,复合这两个条件的表才是候选表。把这两个参数，设计一个好的名字和结构，放到config.yaml文件中。
2.第二步，根据候选表的字段计算组合计算下面的三个指标：
inclusion_rate
jaccard_index
uniqueness
请注意，上面要根据复合键的多个字段组合关联来进行计算，不是单个字段计算后的平均值，而是多个字段组合关联后的结果。
3.第三步，按照config.yaml配置文件中的weights来计算composite_score：
  weights:
    inclusion_rate: 0.30     # 包含率：衡量 from 表的值在 to 表中出现的比例。
    jaccard_index: 0.10      # 杰卡德指数：衡量两个字段值集合的相似度（交集/并集）。
    uniqueness: 0.10         # 唯一度：衡量 to 表字段的唯一性，判断其是否像一个主键/维表键。
    name_similarity: 0.25    # 名称相似度：基于列名的相似程度。
    type_compatibility: 0.25 # 类型兼容性：数据类型是否匹配。
4.最后，根据 composite_score 生成json/md/cypher, 并写入到neo4j中.
5.此外，新需求的增加，可能会影响到output中的json/md/cypher等文件的结构，请根据需要修改。
请分析我上面的需求是否合理，然后结合当前的代码现状，给出修改方案，我同意方案后，再修改代码。\r\n新增配置：\r\n- discovery.composite.min_name_similarity (默认 0.8)\r\n- discovery.composite.min_type_compatibility (默认 0.8)\r\n- discovery.composite.max_columns / max_candidates_per_pair / candidate_sources / allow_mixed_sources\r\n上述参数用于控制复合键候选的生成范围和阈值。
