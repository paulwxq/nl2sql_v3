# 关联字段查找算法详解

## 概述

DB2Graph项目中的关联字段查找算法是一个多阶段、多维度的智能关系发现系统。该算法结合了确定性规则匹配和可选的LLM语义增强，能够从数据库元数据和实际数据中自动发现表之间的潜在关联关系。

## 1. 主键、外键和索引的特殊处理

### 1.1 现有外键的处理
- **直通转换**：已存在的外键约束被直接转换为高置信度关系（confidence=1.0）
- **排除机制**：在候选生成阶段，已存在的外键关系会被排除，避免重复分析
- **关系类型**：外键关系被标记为`foreign_key`类型，具有最高优先级

### 1.2 主键和唯一键的处理
- **目标列限制**：在单列关系候选生成中，目标列（to_column）必须满足以下条件之一：
  - 是主键（`is_primary_key = true`）
  - 是唯一键（`is_unique = true`）
  - 拥有索引标记（`has_index = true`），包括复合索引拆分后的单列索引
  - 具有高唯一性（`uniqueness >= 0.95`）
- **评分加成**：如果目标列是主键或唯一键，名称相似度会获得20%的加成（`name_sim * 1.2`）

### 1.3 索引的处理
- **复合键阈值配置**：新增 `discovery.composite` 节点用于控制复合主键/索引/外键的候选生成，支持 `min_name_similarity` 与 `min_type_compatibility` 阈值、最大列数、来源控制以及是否允许跨来源组合。
- **显式采集**：元数据阶段会抓取所有单列与复合索引并写入 `TableMetadata.indexes`，列对象同步标记 `has_index`。
- **候选优先级**：拥有索引的列（含复合索引拆分后的单列）在候选/目标筛选时自动视为合格，即便不是 PK/UK 也能进入评估。
- **性能与准确性**：索引既用于后续采样的查询优化，也作为结构提示配合唯一性阈值降低误判。

### 1.4 字段注释的参与
目前算法**暂未**使用字段注释信息进行关系判断，主要依赖以下信息：
- 字段名称的语义相似度
- 数据类型兼容性
- 实际数据样本的匹配度
- 数据分布特征（唯一性、空值率等）

## 2. 算法执行的阶段和步骤

### 2.1 第一阶段：元数据读取和预处理
```
步骤1: 读取数据库元数据
├── 获取表结构信息（列、数据类型、约束）
├── 提取现有外键关系
├── 识别主键和唯一键
└── 生成表统计信息

步骤2: 数据采样和画像分析
├── 对每个表进行数据采样（默认1%或最多10000行）
├── 计算列的数据画像（唯一性、空值率、基数等）
├── 识别候选关系列（排除时间戳、审计列等噪声列）
└── 生成数据模式摘要（UUID、邮箱、数值等模式）
```

### 2.2 第二阶段：关系候选生成
```
步骤3: 单列关系候选生成
├── 表对枚举：对所有表进行两两组合（包括双向检查）
├── 列对筛选：
│   ├── 源列筛选：排除噪声列、高空值率列
│   ├── 目标列筛选：必须是PK/UK、has_index列或高唯一性列
│   └── 类型兼容性检查：compatibility >= 0.5（依据 discovery.type_compatibility.groups 配置）
├── 名称相似度计算：
│   ├── 基础算法：标准化、同义词、包含关系、编辑距离
│   └── LLM增强（可选）：针对临界候选进行语义分析
└── 候选对象创建：生成RelationshipCandidate对象

步骤4: 复合键关系候选生成（可选）
├── 弱候选筛选：name_similarity在[0.5, 0.8)的候选
├── 表对分组：按(from_table, to_table)分组
├── 组合生成：尝试2列组合（最多5个组合）
└── 优先模式匹配：检查配置中的优先模式
```

### 2.3 第三阶段：候选关系评估
```
步骤5: 度量计算
├── 数据采样：获取源列和目标列的实际值样本
├── 包含率计算：from_values中有多少在to_values中存在
├── Jaccard系数：交集/并集的比例
├── 唯一度评分：目标列的唯一性程度
├── 样本命中率：快速采样验证
└── 综合评分：基于权重配置的加权平均

步骤6: 决策和过滤
├── 评分排序：按composite_score降序排列
├── 阈值过滤：低于reject阈值的候选被排除
├── 冲突解决：避免重复和冲突的关系
└── 置信度分级：high/medium/low三个级别
```

### 2.4 第四阶段：结果输出和报告
```
步骤7: 结果生成
├── Cypher语句生成：用于Neo4j图数据库
├── 关系对象创建：包含完整的度量信息
└── 统计信息汇总：各类关系的数量统计

步骤8: 报告输出
├── JSON详细报告：包含所有候选和度量信息
├── Markdown摘要报告：人类可读的分析结果
└── 日志记录：详细的执行日志和LLM调用统计
```

## 3. 字段查找的遍历策略

### 3.1 遍历方法：优化的穷举法
算法采用**两层嵌套循环**的遍历策略，但加入了多重优化：

```python
# 外层循环：表对组合
for table1, table2 in combinations(table_names, 2):
    # 双向检查（A->B 和 B->A）
    for from_table, to_table in [(table1, table2), (table2, table1)]:
        # 内层循环：列对组合
        for from_col in from_meta.columns:
            for to_col in to_meta.columns:
                # 预筛选条件
                if not _is_candidate_column(from_col): continue
                if not _is_target_qualified(to_col): continue
                if type_compatibility < 0.5: continue
                # 生成候选...
```

### 3.2 预筛选优化策略
为避免不必要的计算，算法在遍历过程中应用多重预筛选：

**源列筛选条件：**
- 排除JSON/JSONB/ARRAY类型
- 排除噪声列（时间戳、审计列等）
- 排除高空值率列（>80%）

**字段类型过滤规则：**

在`_is_candidate_column()`方法中，算法排除以下字段：

```python
def _is_candidate_column(column, profile):
    # 1. 数据类型排除
    if column.type in [ColumnType.JSON, ColumnType.JSONB, ColumnType.ARRAY]:
        return False
    
    # 2. 名称模式排除
    noise_patterns = [
        '_ts', '_time', 'timestamp', 
        'created_', 'updated_', 'deleted_', 'modified_',
        'create_ts', 'update_ts', 'delete_ts'
    ]
    if any(pattern in column.name.lower() for pattern in noise_patterns):
        return False
    
    # 3. 数据质量排除
    if profile and profile.null_ratio > 0.8:
        return False
```

**被排除的字段类型：**
- JSON、JSONB、ARRAY类型
- 包含时间戳模式的字段名（_ts、_time、timestamp、created_、updated_等）
- 空值率超过80%的字段

**未被排除的字段类型：**
- 数值类型（包括INTEGER、BIGINT、REAL、DOUBLE等）
- 字符串类型（VARCHAR、TEXT等）
- 日期时间类型（DATE、TIMESTAMP等）

**目标列筛选条件：**
- 必须是主键、唯一键、具有索引标记的列，或高唯一性列（>=95%）
- 类型必须与源列兼容（compatibility >= 0.5，来自 discovery.type_compatibility.groups）

**名称相似度门槛：**
- 基础相似度必须达到配置阈值（默认0.6）
- 或者类型完全兼容（compatibility >= 0.8）

### 3.3 复杂度分析
- **时间复杂度**：O(T² × C²)，其中T是表数量，C是平均列数
- **实际优化**：通过预筛选，实际计算的候选数量远小于理论上限
- **并发控制**：评估阶段采用批处理（默认3个并发）避免连接池耗尽

## 4. 评分系统的具体方法

### 4.1 评分维度和权重配置
```yaml
weights:
  inclusion_rate: 0.30      # 包含率（最重要）
  jaccard_index: 0.15       # Jaccard系数
  uniqueness: 0.10          # 唯一度
  name_similarity: 0.20     # 名称相似度
  type_compatibility: 0.20  # 类型兼容性
  name_complexity: 0.05     # 名称复杂度
```
附注：所有导出的指标（composite_score、inclusion_rate、jaccard_index、uniqueness_score、type_compatibility）统一保留最多两位小数，便于人工审阅。

### 4.2 各维度的具体计算方法

#### 4.2.1 包含率 (Inclusion Rate)
```python
def _calculate_inclusion_rate(from_values, to_values):
    """计算FROM表中有多少值在TO表中存在"""
    # 排除None值
    from_clean = {v for v in from_values if v is not None}
    to_clean = {v for v in to_values if v is not None}
    
    if not from_clean:
        return 0.0
    
    intersection = from_clean & to_clean
    return len(intersection) / len(from_clean)
```

#### 4.2.2 Jaccard系数
```python
def _calculate_jaccard_index(set1, set2):
    """计算交集/并集的比例"""
    # 排除None值
    set1_clean = {v for v in set1 if v is not None}
    set2_clean = {v for v in set2 if v is not None}
    
    intersection = set1_clean & set2_clean
    union = set1_clean | set2_clean
    
    return len(intersection) / len(union) if union else 0.0
```

#### 4.2.3 名称相似度计算

**关于表名的参与：**
- **基础算法（确定性）**：只比较字段名相似度，不使用表名信息
- **LLM增强算法**：表名作为上下文信息传递给LLM，但LLM仍然评估字段名相似度

**基础算法（确定性）：**
```python
# 只传递字段名，不涉及表名
name_sim = self._calculate_name_similarity(from_col.name, to_col.name)
```

1. **标准化**：转换为小写，去除下划线、连字符、空格
2. **完全匹配**：标准化后完全相同 → 1.0
3. **同义词匹配**：在配置的同义词组中 → 0.9
4. **包含关系**：一个名称包含另一个 → 0.8
5. **特殊模式**：`user_id`匹配`id` → 0.7
6. **编辑距离**：使用SequenceMatcher计算相似度（兜底方案）

##### 编辑距离详解
当上述所有规则都不适用时，算法使用**编辑距离**作为最后的兜底方案来计算两个字段名的相似度。

**什么是编辑距离？**
编辑距离（Edit Distance），也称为Levenshtein距离，是指将一个字符串转换为另一个字符串所需的最少编辑操作次数。允许的编辑操作包括：
- **插入**一个字符
- **删除**一个字符  
- **替换**一个字符

**SequenceMatcher的工作原理：**

`SequenceMatcher` 是Python标准库 `difflib` 中的一个类，专门用于比较序列（如字符串）的相似性。它基于**Ratcliff-Obershelp算法**，这是一种启发式的字符串匹配算法。

**核心工作机制：**
1. **寻找最长公共子序列**：算法递归地找到两个字符串中最长的连续匹配块
2. **分治策略**：将字符串分割成更小的部分，分别计算匹配度
3. **匹配块累积**：将所有找到的匹配块的长度累加起来

```python
from difflib import SequenceMatcher

# 在标准化后的名称上计算相似度
def _calculate_name_similarity(name1, name2):
    # 1. 标准化处理
    name1_norm = name1.lower().replace('_', '').replace('-', '').replace(' ', '')
    name2_norm = name2.lower().replace('_', '').replace('-', '').replace(' ', '')
    
    # 2. 其他规则检查...
    
    # 3. 编辑距离计算（兜底方案）
    return SequenceMatcher(None, name1_norm, name2_norm).ratio()
```

**SequenceMatcher的内部工作流程：**
```python
# 示例：比较 "customerid" 和 "customercode"
matcher = SequenceMatcher(None, "customerid", "customercode")

# 1. 寻找匹配块
matching_blocks = matcher.get_matching_blocks()
# 结果可能是：[Match(a=0, b=0, size=8), Match(a=10, b=12, size=0)]
# 表示从位置0开始，有8个字符匹配："customer"

# 2. 计算总匹配字符数
total_matches = sum(block.size for block in matching_blocks)
# total_matches = 8

# 3. 应用ratio公式
ratio = 2 * total_matches / (len("customerid") + len("customercode"))
# ratio = 2 * 8 / (10 + 12) = 16 / 22 ≈ 0.727
```

**SequenceMatcher.ratio()计算公式：**
```
ratio = 2 * M / T
```
其中：
- `M` = 匹配字符的总数量（所有匹配块的大小之和）
- `T` = 两个序列的总长度（len(seq1) + len(seq2)）

**为什么使用这个公式？**
- **对称性**：`ratio(A, B) = ratio(B, A)`
- **归一化**：结果总是在 [0, 1] 区间内
- **长度敏感**：相同的匹配在较短的字符串中会得到更高的分数

**SequenceMatcher vs 传统编辑距离的区别：**

| 特性 | SequenceMatcher (Ratcliff-Obershelp) | 传统编辑距离 (Levenshtein) |
|------|--------------------------------------|----------------------------|
| **算法基础** | 最长公共子序列 + 分治 | 动态规划 |
| **匹配类型** | 寻找连续的匹配块 | 逐字符操作（插入/删除/替换） |
| **计算复杂度** | O(n²) 平均情况 | O(nm) 严格 |
| **对长匹配的偏好** | 更偏好长的连续匹配 | 对所有操作等权重 |
| **适用场景** | 字符串相似度比较 | 拼写纠错、序列对齐 |

**在DB2Graph中选择SequenceMatcher的原因：**
1. **更适合字段名比较**：字段名通常有共同的前缀或后缀（如 "customer_id" vs "customer_code"）
2. **性能考虑**：在大量字段比较中，SequenceMatcher的性能表现更稳定
3. **Python原生支持**：作为标准库的一部分，无需额外依赖

**实际计算示例：**
```python
# 示例1：相似字段名
name1 = "customer_id"      # 标准化后: "customerid" 
name2 = "customer_code"    # 标准化后: "customercode"
# SequenceMatcher会找到公共子序列 "customer"
# ratio ≈ 0.8 (较高相似度)

# 示例2：轻微拼写差异
name1 = "username"         # 标准化后: "username"
name2 = "user_name"        # 标准化后: "username" 
# 完全匹配，但如果是 "usrname" vs "username"
# ratio ≈ 0.88 (一个字符差异)

# 示例3：不相关字段
name1 = "price"           # 标准化后: "price"
name2 = "category"        # 标准化后: "category"
# 几乎没有公共字符
# ratio ≈ 0.0 (低相似度)
```

**为什么使用编辑距离作为兜底？**
1. **处理拼写变异**：处理字段命名中的轻微拼写差异
2. **连续相似度**：提供0-1之间的连续相似度值，而非离散的规则匹配
3. **语言无关**：对不同命名风格和语言都有效
4. **计算高效**：SequenceMatcher使用优化的算法，计算复杂度合理

**算法的层次结构：**
```
名称相似度计算流程：
│
├── 1. 完全匹配 → 1.0 (最高优先级)
├── 2. 同义词匹配 → 0.9 (业务规则)
├── 3. 包含关系 → 0.8 (常见模式)
├── 4. 特殊模式 → 0.7 (如 user_id ↔ id)
└── 5. 编辑距离 → 0.0-1.0 (兜底计算)
```

这种分层设计确保了既能捕获明确的业务规则，又能处理各种边界情况。

**LLM增强算法（可选）：**
```python
# 传递表名作为上下文，但目标仍是评估字段名相似度
enhanced = await self.calculate_name_similarity_enhanced(
    from_column_name,
    to_column_name,
    from_table,    # 表名作为上下文
    to_table,      # 表名作为上下文
    metadata=metadata,
    profiles=profiles
)
```
- 仅对"临界候选"调用LLM（name_similarity在[min_score-0.1, min_score+0.2]窗口内）
- 提供丰富的上下文信息：**表名**、类型、样例值、数据模式
- 表名作用：帮助LLM理解字段的业务语义，不直接计算表名相似度
- 结合基础分数：`0.7 * base_score + 0.3 * llm_score`
- LLM只能增强，不能降低基础分数

#### 4.2.4 类型兼容性
```python
def _calculate_type_compatibility(type1, type2):
    """计算数据类型兼容性"""
    if type1 == type2:
        return 1.0  # 完全相同
    elif type1.is_compatible_with(type2):
        return 0.8  # 兼容类型（如int和bigint）
    else:
        return 0.0  # 不兼容
```

#### 4.2.5 名称复杂度
复杂度越高表示字段越具体、越有业务含义：
```python
def calculate_name_complexity(field_name):
    """计算字段名复杂度 (0.0-1.0)"""
    # 1. 黑名单过滤（通用字段如id、name返回0.0）
    if _is_common_field(field_name):
        return 0.0
    
    # 2. 基础长度评分
    base_score = _calculate_base_score_by_length(field_name)
    
    # 3. 语义加成
    semantic_bonus = 0.0
    if '_' in field_name or _has_camel_case(field_name):
        semantic_bonus += 0.2  # 命名风格加成
    if _has_business_suffix(field_name):
        semantic_bonus += 0.15  # 业务后缀加成
    if _count_meaningful_words(field_name) >= 2:
        semantic_bonus += 0.1   # 复合词加成
    
    return min(1.0, base_score + semantic_bonus)
```

### 4.3 综合评分计算
```python
def calculate_composite_score(metrics, weights):
    """计算综合评分"""
    score = (
        metrics.inclusion_rate * weights.inclusion_rate +
        metrics.jaccard_index * weights.jaccard_index +
        metrics.uniqueness_score * weights.uniqueness +
        metrics.candidate.name_similarity * weights.name_similarity +
        metrics.candidate.type_compatibility * weights.type_compatibility +
        metrics.candidate.name_complexity * weights.name_complexity
    )
    return min(1.0, max(0.0, score))
```

### 4.4 置信度分级
```yaml
decision:
  auto_accept: 0.90     # >= 0.90 为高置信度
  review_required: 0.80 # >= 0.80 为中置信度
  reject: 0.80          # < 0.80 为低置信度/拒绝
```

## 5. LLM调用的时机和结果整合

### 5.1 LLM调用的触发条件
LLM增强功能采用**精准触发**策略，仅在以下情况下调用：

1. **LLM可用性检查**：`llm_controller.is_available() == True`
2. **预算控制**：未超过配置的最大调用次数
3. **临界候选筛选**：
   - 类型兼容性 >= 0.8（类型较匹配）
   - 名称相似度在增强窗口内：`[min_score + win_left, min_score + win_right]`
   - 默认窗口：`[0.6-0.1, 0.6+0.2] = [0.5, 0.8]`

### 5.2 LLM调用的具体场景

#### 5.2.1 名称相似度增强
**调用时机**：当基础名称相似度在临界区间时
**输入上下文**：
```python
context = {
    'table1': from_table,
    'table2': to_table,
    'type1': column1_type,
    'type2': column2_type,
    'type_compatibility': compatibility_score,
    'sample_values1': [sample1, sample2, sample3],  # 前3个非空样例
    'sample_values2': [sample1, sample2, sample3],
    'pattern1': "uuid-like; small-enum",             # 数据模式摘要
    'pattern2': "numeric-like",
    'profile1_signals': {                            # 离散化信号
        'uniqueness': 'high',
        'null_ratio': 'low',
        'cardinality': 'high',
        'is_likely_unique': True
    }
}
```

**LLM提示模板**：
```
请评估两个数据库列在业务语义上的相似度（0-1）。

列名1: user_id
列名2: id
表1: user_profile
表2: users
类型1: integer
类型2: bigint
类型兼容性: 0.80
样例值1: 1001, 1002, 1003
样例值2: 1, 2, 3
模式摘要1: numeric-like
模式摘要2: numeric-like; small-enum

请考虑：
1. 列名语义、命名规范与同义词关系
2. 数据类型与类型兼容性
3. 实际样例值的内容特征
4. 数据模式摘要（如 uuid/date/numeric/小枚举 等）
5. 画像信号（唯一性、高低基数、空值比例）

请只返回一个0到1之间的小数，表示相似度分数。
直接返回数字，不要其他文字。

答案：
```

#### 5.2.2 业务规则分析
**调用时机**：在BusinessEnhancer中，对候选关系进行业务合理性分析
**返回结果**：
```json
{
    "business_score": 0.85,
    "relationship_type": "one_to_many",
    "confidence": 0.80,
    "reasoning": "user_profile.user_id指向users.id符合典型的用户档案关联模式"
}
```

### 5.3 LLM结果的整合策略

#### 5.3.1 名称相似度增强整合
```python
def integrate_llm_name_similarity(base_score, llm_score):
    """整合LLM增强的名称相似度"""
    # 权重分配：确定性算法70%，LLM增强30%
    enhanced_score = 0.7 * base_score + 0.3 * llm_score
    
    # LLM只能增强，不能降低基础分数
    final_score = max(base_score, enhanced_score)
    
    return final_score
```

#### 5.3.2 业务规则整合
```python
def integrate_business_enhancement(metrics, business_analysis):
    """整合业务规则分析结果"""
    # 业务评分作为额外维度加入综合评分
    if business_analysis['enhanced']:
        # 可以调整最终的置信度级别
        if business_analysis['business_score'] > 0.8:
            metrics.confidence_level = "high"
        # 添加业务推理信息
        metrics.business_reasoning = business_analysis['reasoning']
```

### 5.4 LLM调用的并发控制和容错
```python
# 并发控制
max_concurrency = config.llm.max_concurrency or 3
semaphore = asyncio.Semaphore(max_concurrency)

# 超时控制
timeout_ms = config.llm.timeout_ms or 30000

# 批量并发增强
async def enhance_critical_candidates(critical_candidates):
    tasks = []
    for candidate in critical_candidates:
        task = enhance_single_candidate(candidate)
        tasks.append(task)
    
    # 并发执行，异常隔离
    results = await asyncio.gather(*tasks, return_exceptions=True)
    
    # 处理结果和异常
    for result in results:
        if isinstance(result, Exception):
            logger.debug(f"LLM增强任务异常: {result}")
            # 保持原始分数，不影响整体流程
        else:
            # 应用增强结果
            apply_enhancement(result)
```

## 6. 输出结果文件的产生和修改时间点

### 6.1 JSON详细报告 (`analysis_report_YYYYMMDD_HHMMSS.json`)

**产生时间点**：
- 分析流程完全结束后
- 在`Orchestrator._save_report()`方法中生成
- 时机：所有关系决策完成，但在显示最终统计之前

**文件内容**：
```json
{
  "run_id": "uuid",
  "timestamp": "2025-09-17T00:52:43.726673",
  "database": "数据库名",
  "db_schema": "schema名",
  "tables_analyzed": 10,
  "columns_analyzed": 124,
  "candidates_generated": 878,
  "relationships_discovered": 28,
  "relationships": [
    {
      "from_table": "source_table",
      "from_columns": ["column1"],
      "to_table": "target_table", 
      "to_columns": ["column2"],
      "relationship_type": "inferred",
      "join_type": "LEFT",
      "metrics": {
        "candidate": {...},
        "inclusion_rate": 1.0,
        "jaccard_index": 0.99,
        "uniqueness_score": 1.0,
        "sample_hit_rate": 1.0,
        "composite_score": 0.994,
        "confidence_level": "high"
      },
      "source": "auto_detected",
      "confidence": 0.994,
      "verified": true
    }
  ],
  "execution_time_seconds": 45.2,
  "config": {
    "sampling_ratio": 0.01,
    "max_rows": 10000,
    "thresholds": {...},
    "discovery_strategies": [...]
  }
}
```

**修改时间点**：
- **首次创建**：分析完成时一次性写入
- **不会修改**：文件一旦创建就不再修改
- **版本控制**：每次运行产生新的时间戳文件

### 6.2 Markdown摘要报告 (`analysis_summary_YYYYMMDD_HHMMSS.md`)

**产生时间点**：
- 紧跟在JSON报告生成之后
- 在`Orchestrator._save_summary_report()`方法中生成
- 基于JSON报告的数据生成人类可读的摘要

**文件内容结构**：
```markdown
# DB2Graph 分析报告

**运行ID**: 882b5143-a8c5-4161-8160-64d629d2f7b9
**时间**: 2025-09-17T00:52:43.726673
**数据库**: highway_db (schema: public)
**执行时间**: 45.2秒

## 统计信息

- 分析表数量: 10
- 分析列数量: 124
- 生成候选数: 878
- 发现关系数: 28

## 发现的关系

### 高置信度关系 (15个)

- **bss_service_area_mapper** → **bss_service_area**: service_area_id → id (置信度: 99.43%)
- **bss_branch** → **bss_section_route**: section_route_id → id (置信度: 98.49%)
- ...

### 中置信度关系 (8个)

- table1 → table2: col1 → col2 (置信度: 85.20%)
- ...

### 低置信度关系 (5个)

- table3 → table4: col3 → col4 (置信度: 82.10%)
- ...

## 输出文件

- Cypher 文件: `output/cypher/relationships_uuid.cypher`
```

**修改时间点**：
- **首次创建**：JSON报告生成后立即创建
- **不会修改**：文件创建后不再修改
- **格式特点**：专注于高层次统计和最重要的关系

### 6.3 Cypher文件 (`relationships_UUID.cypher`)

**产生时间点**：
- 在`Orchestrator._output_results()`方法中
- 关系决策完成后，报告生成之前
- 仅在`output_mode`为`cypher`或`both`时生成

**文件内容示例**：
```cypher
// DB2Graph Auto-generated Relationships
// Generated at: 2025-09-17T00:52:43

// High Confidence Relationships
CREATE (:Table {name: 'bss_service_area_mapper'})-[:REFERENCES {
  from_columns: ['service_area_id'],
  to_columns: ['id'],
  confidence: 0.994,
  inclusion_rate: 1.0,
  source: 'auto_detected'
}]->(:Table {name: 'bss_service_area'});
```

**修改时间点**：
- **首次创建**：每次分析运行时创建
- **不会修改**：文件创建后不再修改
- **存储位置**：`output/cypher/`目录

### 6.4 日志文件 (`db2graph.log`)

**产生时间点**：
- **持续写入**：从应用启动开始，贯穿整个分析过程
- **实时更新**：每个操作步骤都会产生日志记录

**关键日志时间点**：
```
[开始] 元数据读取
[进行] 数据采样和画像分析  
[进行] 候选关系生成
[进行] LLM增强调用（如果启用）
[进行] 候选关系评估
[进行] 关系决策
[完成] 结果输出
[完成] 报告生成
```

**LLM调用日志**：
```
[DEBUG] LLM 请求: provider=deepseek, function=suggest_name_similarity
[DEBUG] LLM 响应: provider=deepseek, function=suggest_name_similarity  
[INFO] LLM增强完成: table1.col1 -> table2.col2 基础=0.65, 增强=0.75, 最终=0.72
```

### 6.5 文件生命周期总结

```
时间轴: 分析流程进行时
│
├── T0: 应用启动
│   └── 开始写入日志文件
│
├── T1-T5: 分析执行阶段  
│   ├── 持续写入日志
│   ├── LLM调用日志（如果启用）
│   └── 进度和调试信息
│
├── T6: 关系决策完成
│   └── 生成Cypher文件（如果配置）
│
├── T7: 分析结果汇总
│   ├── 创建JSON详细报告
│   └── 创建Markdown摘要报告
│
└── T8: 分析完成
    ├── 输出最终统计信息到日志
    ├── 显示LLM使用统计（如果启用）
    └── 程序结束
```

**文件持久性**：
- 所有输出文件一旦创建就不再修改
- 每次运行产生独立的时间戳文件
- 支持历史版本的对比和追溯
- 日志文件可能被轮转（根据配置）

## 总结

DB2Graph的关联字段查找算法是一个多层次、多维度的智能系统，具有以下特点：

1. **渐进式精确度**：从粗粒度筛选到精细化评估，层层递进
2. **多维度评分**：结合名称、类型、数据分布等多个维度
3. **智能优化**：通过预筛选大幅减少计算量
4. **可选增强**：LLM增强功能可提升语义理解能力
5. **全面记录**：详细的日志和报告支持结果追溯和优化

该算法在保证较高准确率的同时，通过各种优化策略确保了在大型数据库上的可行性和效率。
