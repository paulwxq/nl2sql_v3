# State 瘦身优化方案 v1.3 微调总结

## 修正日期
2025-11-12

## 修正内容

### ✅ 1. grain_hint 的一致性修正

**问题**：前文 C2 建议不保留 grain_hint，后文"预格式化/保留元数据"又例示了保留。

**修正**：
- §2.5 数据分类表格：删除 grain_hint，只保留 time_col_hint
- 保留元数据示例：移除 grain_hint，添加注释说明已删除
- 所有JSON示例：已统一（部分示例可能需要手动检查）

**修正位置**：
- 行 435：数据分类表格
- 行 580-602：保留元数据说明

---

### ✅ 2. validation_history 的表现形态统一

**问题**：v1.3 前文写"只限制最后3条，结构保持不变"，附录示例又用了 sql_hash/sql_preview。

**修正**：
- 附录两处JSON示例（基础版和预格式化版）：改为完整 SQL 和 explain_plan
- 配置示例：删除 validation_sql_preview 配置项
- 添加注释：明确"最多保留最近 3 条，结构完整不变"

**修正位置**：
- 行 1621-1634：优化后（基础版）JSON示例
- 行 1706-1719：优化后（预格式化版）JSON示例
- 行 1774：配置文件示例

**最终形态**：
```json
"validation_history": [
  {
    "iteration": 1,
    "sql": "SELECT ...",  // 完整SQL
    "result": {
      "valid": true,
      "errors": [],
      "warnings": ["检测到顺序扫描（Seq Scan），可能影响性能"],
      "layer": "all_passed",
      "explain_plan": "Aggregate  (cost=39.61...) ..."  // 保留
    }
  }
  // 最多保留最近 3 条，结构完整不变
]
```

---

### ✅ 3. 删除时机的表述更精确

**问题**：不同类型字段的删除时机表述不够清晰。

**新增内容**：§2.0 删除时机说明（行 111-146）

#### 📌 阶段1：检索节点输出时删除（不写入 State）

**适用场景**：纯检索中间数据，下游节点完全不使用

**字段列表**（6个）：
- `schema_context.columns` ✅
- `schema_context.tables` ✅
- `schema_context.candidate_fact_tables` ✅
- `schema_context.candidate_dim_tables` ✅
- `schema_context.candidate_bridge_tables` ✅
- `schema_context.table_similarities` ✅

**实施方式**：在 `schema_retrieval_node` 的 `_slim_schema_context()` 函数中直接不写入。

**优势**：减少节点间传递的数据量，提升性能。

#### 📌 阶段2：统一瘦身口删除（检查点/输出前）

**适用场景**：仍参与同轮流程的过程字段或需要裁剪的字段

**字段列表**：
- `generated_sql`（验证节点使用后删除）
- `validation_history`（限制数量至最后3条）
- `validation_result.explain_plan`（内部使用后删除）
- `parse_result.keywords/signals/evidence`（调试字段）
- `join_plans.edges[].cost/constraint_name`（精简结构）
- `table_cards[].attrs`（精简结构）

**实施方式**：在 `slim_state_for_checkpoint()` 统一函数中处理。

**优势**：集中管理，统一控制，易于维护。

---

## 关联修改

### §2.1 类型C - 处理方式更新（行 191-195）
- 明确推荐在检索节点侧直接不写入（阶段1）
- 备选方案：统一瘦身口删除（阶段2）

### §3.1 节点级优化 - 标题更新（行 998-1004）
- 从"备选方案"改为"推荐：阶段1实施"
- 明确删除检索中间数据的目标

---

## 文档版本

**当前版本**：v1.3（用户反馈修订版，已完成微调）

---

## 总结

所有3处不一致问题已修正：
1. ✅ grain_hint 已在全文统一为"删除"
2. ✅ validation_history 已统一为"保留最后3条，结构完整不变"
3. ✅ 删除时机已明确区分为两阶段策略

文档现在保持了内部一致性，可以作为实施指南使用。

