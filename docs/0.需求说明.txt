我已经完成了SQL子图的开发和测试工作，现在决定完成父图的Fast Path的地方。
就是在  @30.0.nl2sql_father_graph_architecture.html 左侧的线路图，这是我们开发的第一阶段。
在测试中，我会挑选简单的问题，让Router 会路由到 FastPath.

目前，第一阶段要开发的节点：
0.父State的开发，请阅读代码获取子State的定义，参考 @30.1.State 技术参考.md  文档设计父State的定义。
1.Router 问题分类节点。
2.调用"SQL生成子图"：SQL生成子图已经开发完成，SQL生成子图的定义请参考 @30.2.SQL 生成子图 State 定义.md ，如果它的State或者接口需要改造，也是允许的，请提出来。
3."SQL执行节点"：支持单条SQL执行，也支持多条SQL执行。
4.Summary 节点：总结执行结果，并返回结果。

在设计中需要注意的事项：
1.在html图中，FastPath(左侧)使用了“简单总结可选LLM”，右侧线路使用了“Summarizer LLM总结”，事实上我认为两条线路它们可以共享使用同一个“Summarizer LLM总结”节点。
2.虽然当前的是第一阶段的设计，但仍然要考虑有些节点是否可以在后续开发中共享，比如SQL执行节点，它在左侧的FastPath和右侧的复杂Path都会用到，希望你在设计的时候，要考虑到这一点。
3.项目中使用Langgraph v1.0.x 版本，请尽量使用新特性。
4.第一阶段开发目标指的是截图中左侧的链路。
---
请按照上面的建议，先完成设计当前第一阶段的概要设计，序号使用 31.，以md文档存储在 docs/ 目录下。