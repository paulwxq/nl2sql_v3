# 测试主键定义解析功能的配置文件
project_name: "test_pk"
description: "Primary Key Definition Test"

source_type: "ddl"  # 使用 DDL 模式

# DDL 配置
ddl:
  ddl_dir: "output/test_pk/ddl"

# 输出配置
output:
  base_dir: "output/test_pk"

# 数据库配置（不使用）
database: null

